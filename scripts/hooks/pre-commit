#!/usr/bin/env sh
# Git pre-commit hook entry point.
# Dispatches to the Credential Guard secret scanner.
# To add more pre-commit checks in the future, chain them here.
exec "$(dirname "$0")/credential-guard" "$@"
